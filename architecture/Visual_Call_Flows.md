# PlaceWell Visual Architecture and Call Flows

This document shows the implemented PlaceWell architecture as of August 2026. It separates what runs on the phone, what is hosted on Linode, what is stored in Firebase/Firestore, and what runs only as internal operator tooling.

## 1. System boundary map

```mermaid
flowchart LR
    subgraph CUSTOMER["Customer device"]
        CAM["Phone camera"]
        APP["PlaceWell mobile app<br/>React Native / Expo"]
        LOCAL[("AsyncStorage + local photos<br/>user label contents, rooms,<br/>zones, notes, freshness")]
    end

    subgraph LINODE["Linode VM - placewell.app"]
        APACHE["Apache<br/>TLS + reverse proxy"]
        QR["PlaceWellQRService<br/>FastAPI :8000"]
        UI["PlaceWellUI<br/>FastAPI :8080"]
        PDF["PlaceWellPdfGenerator<br/>Python library"]
        FILES[("Generated PDF files")]
    end

    subgraph FIREBASE["Google Firebase / GCP"]
        FS[("Cloud Firestore<br/>qr_codes + orders")]
    end

    subgraph OPERATOR["PlaceWell operator"]
        BROWSER["Password-protected<br/>operator browser"]
        ADMIN["PlaceWellAdmin<br/>local config tool"]
    end

    subgraph STORES["Distribution"]
        APPLE["Apple App Store"]
        PLAY["Google Play"]
    end

    CAM -->|"scan HTTPS QR"| APACHE
    CAM -->|"Universal/App Link<br/>when app is installed"| APP
    APP <-->|"read/write personal inventory"| LOCAL
    APP -->|"HTTPS lookup/order APIs"| APACHE
    APACHE --> QR
    QR <-->|"Admin SDK reads/writes"| FS
    BROWSER -->|"HTTPS Basic Auth"| APACHE
    APACHE --> UI
    UI -->|"internal allocation API"| QR
    UI --> PDF
    PDF --> FILES
    ADMIN -.->|"updates shared source/config<br/>before deployment/build"| APP
    ADMIN -.-> UI
    ADMIN -.-> PDF
    QR -->|"download fallback links"| APPLE
    QR -->|"download fallback links"| PLAY
```

### What is and is not in Firestore

| Data | Current location | Sent to Firestore? |
|---|---|---|
| Issued label ID, order ID, printed name, category, SKU, image key, status, scan counters | Firestore through QR Service | Yes |
| Order allocation summary | Firestore through QR Service | Yes |
| User-entered contents, notes, room, zone, freshness dates | Mobile `AsyncStorage` | No |
| User photos attached to labels | Mobile device/app storage | No |
| Generated label and manifest PDFs | Linode filesystem | No |
| Category/template/style source configuration | Source repositories and UI CSV files | No |

The mobile app does **not** connect directly to Firestore. All server metadata access goes through the QR Service on Linode.

## 2. Producing a physical label order

```mermaid
sequenceDiagram
    actor Operator
    participant UI as PlaceWellUI<br/>Linode
    participant Builder as order_builder.py
    participant QR as QR Service<br/>Linode
    participant HMAC as Server HMAC utility
    participant FS as Firestore
    participant PDF as PDF Generator<br/>Linode
    participant Disk as Linode PDF output

    Operator->>UI: Submit category, names, SKU, style and footer
    UI->>Builder: Build named labels + Order QR + optional blanks
    Builder-->>UI: Allocation item skeletons
    UI->>QR: POST /api/qr/allocate<br/>bearer-authenticated
    loop Each physical label
        QR->>FS: Check generated 6-character ID is unused
        QR->>HMAC: Compute 4-character signature
        HMAC-->>QR: https://placewell.app/s/ID-SIGNATURE
    end
    QR->>FS: Atomic batch write qr_codes + order summary
    FS-->>QR: Commit succeeds or entire batch fails
    QR-->>UI: item_id -> QR URL allocations
    UI->>PDF: Complete order payload with QR URLs
    PDF->>Disk: Render label-sheet PDF + manifest PDF
    UI-->>Operator: Password-protected download links
```

**Important behavior**

- HMAC is generated by the QR Service, not by the operator UI.
- Firestore allocation is atomic: an order does not partially claim label IDs.
- The PDF embeds the signed HTTPS URL. It does not query Firestore.
- PlaceWellAdmin is not in the live request path; it updates shared configuration used by later builds/deployments.

## 3. In-app camera scan of one label

```mermaid
sequenceDiagram
    actor User
    participant Camera as Expo Camera
    participant App as ScannerScreen
    participant HMAC as Local HMAC verifier
    participant Local as AsyncStorage
    participant QR as QR Service<br/>Linode
    participant FS as Firestore

    User->>Camera: Scan printed QR
    Camera-->>App: https://placewell.app/s/ID-SIGNATURE
    App->>HMAC: Parse ID/signature and verify locally
    alt Signature is invalid
        HMAC-->>App: Reject
        App-->>User: Invalid PlaceWell label
    else Signature is valid
        App->>Local: Add validation queue entry<br/>and find label by ID
        alt Label already exists locally
            App->>QR: Optional metadata enrichment lookup
            QR->>FS: Read qr_codes/ID
            FS-->>QR: Issued metadata
            QR-->>App: Issued name/category/SKU metadata
            App->>Local: Preserve/enrich issued metadata<br/>and update lastViewedAt
            App-->>User: Spice -> Recall<br/>Other -> Detail
        else First scan on this device
            App->>QR: GET /api/qr/lookup/ID-SIGNATURE
            QR->>QR: Verify bearer token + HMAC
            QR->>FS: Read qr_codes/ID
            FS-->>QR: Issued metadata
            QR-->>App: Name/category/SKU/order flag/defaults
            App-->>User: Open Label Setup
        end
    end
```

**Failure behavior:** the initial authenticity check works offline. If the later metadata lookup fails, the app can continue with the name encoded in the QR URL or with empty/default setup fields. Firestore never receives the user's personal label contents.

## 4. Scan with the phone's native camera

The first hop depends on whether iOS/Android successfully associates `placewell.app` with the installed app.

```mermaid
flowchart TD
    START["Native camera scans<br/>https://placewell.app/s/ID-SIGNATURE"]
    OS{"Universal Link / App Link<br/>association succeeds?"}
    APP["OS opens PlaceWell directly<br/>with full HTTPS URL"]
    LOCAL["App verifies HMAC locally"]
    WEB["Apache -> QR Service /s/..."]
    FS["QR Service checks Firestore:<br/>ID exists, active/revoked,<br/>label vs Order QR"]
    PAGE["Server landing page"]
    SCHEME["JavaScript attempts<br/>placewell://scan/ID or<br/>placewell://order/ORDER_ID"]
    DOWNLOAD["App Store / Google Play<br/>download fallback"]
    ROUTE["App Navigator stores pending link<br/>and mounts Scanner"]
    PROCESS["Scanner runs label lookup,<br/>local-existence check and routing"]

    START --> OS
    OS -->|"Yes"| APP --> LOCAL --> PROCESS
    OS -->|"No / app absent"| WEB --> FS --> PAGE
    PAGE -->|"app installed but association missed"| SCHEME --> ROUTE --> PROCESS
    PAGE -->|"app not installed"| DOWNLOAD
```

### Trust distinction

- **Direct HTTPS App/Universal Link:** the app still has `ID-SIGNATURE`, so it verifies HMAC locally.
- **Server fallback custom link:** the QR Service first resolves the ID in Firestore, then sends `placewell://scan/ID` without the signature. The app marks this path as server-validated and does not repeat HMAC verification.
- The public `/s/` page determines active/revoked/not-found state from Firestore and records a best-effort camera scan count.

## 5. Saving, recalling and editing a label

```mermaid
sequenceDiagram
    actor User
    participant Screen as Setup / Detail / Recall
    participant Storage as storage.js
    participant Local as AsyncStorage
    participant Photos as Device photo storage
    participant FS as Firestore

    User->>Screen: Enter name, room, zone, items,<br/>notes, freshness and optional photo
    opt Take or choose photo
        Screen->>Photos: Camera/photo-library operation
        Photos-->>Screen: Local photo URI
    end
    Screen->>Storage: saveLabel(label)
    Storage->>Local: Insert/update local label record
    Local-->>Storage: Saved
    Storage-->>Screen: Success
    Screen-->>User: Detail/Recall/Home reflects update
    Note over Screen,FS: No personal label contents or photo<br/>are written to Firestore in v1
```

An existing label is routed locally:

- `category === "spice"` -> `LabelRecall`
- every other category -> `LabelDetail`
- edit/save updates the same local record and preserves immutable issued metadata such as physical SKU and printed-name information.

## 6. Order QR bulk import

```mermaid
sequenceDiagram
    actor User
    participant App as ScannerScreen
    participant QR as QR Service<br/>Linode
    participant FS as Firestore
    participant Bulk as BulkImportScreen
    participant Local as AsyncStorage

    User->>App: Scan the physical Order QR
    alt In-app camera receives printed /s/ID-SIGNATURE URL
        App->>App: Validate HMAC and check local label
        App->>QR: GET /api/qr/lookup/ID-SIGNATURE
        QR->>FS: Resolve physical label
        FS-->>QR: is_order_qr + order_id
        QR-->>App: Order QR metadata
    else Server fallback already supplied placewell://order/ORDER_ID
        App->>App: Consume server-resolved order deep link
    end
    App->>QR: GET /api/qr/order/ORDER_ID
    QR->>FS: Query active qr_codes where order_id matches
    FS-->>QR: All issued labels in the order
    QR-->>App: Label list + defaults + metadata
    App->>Bulk: Open grouped setup screen
    User->>Bulk: Adjust global/per-label room and zone
    User->>Bulk: Create labels
    Bulk->>Local: bulkCreateLabels(...)
    Local-->>Bulk: Created count + skipped duplicates
    Bulk-->>User: Success -> Home
```

Firestore is read to reconstruct what was physically issued in the order. The imported, user-configured copies are then stored locally on that device.

## 7. Configuration and deployment flow

```mermaid
flowchart LR
    ADMIN["PlaceWellAdmin<br/>local tool"]
    CONFIG["Shared source/config<br/>templates, styles, categories"]
    APPBUILD["Expo/EAS app build"]
    DEPLOY["Deployment script / operator"]
    LINODE["Linode<br/>QR Service + UI + PDF code"]
    USER["Installed mobile app"]

    ADMIN --> CONFIG
    CONFIG --> APPBUILD --> USER
    CONFIG --> DEPLOY --> LINODE
```

This is a build/deployment flow, not a runtime API flow. A configuration change reaches customers only after the affected app is rebuilt or the affected Linode service is deployed.

## 8. Runtime ownership summary

| Capability | Primary component | Supporting systems |
|---|---|---|
| QR authenticity for in-app scan | Mobile HMAC verifier | Signed URL generated by QR Service |
| Public scan landing/download page | QR Service on Linode | Apache, Firestore |
| Label metadata lookup | QR Service on Linode | Firestore |
| Personal inventory CRUD/search | Mobile app | AsyncStorage/device photos |
| Order QR bulk import | Mobile app + QR Service | Firestore, AsyncStorage |
| QR ID allocation | QR Service on Linode | Firestore atomic batch, HMAC |
| Order form | PlaceWellUI on Linode | Apache Basic Auth |
| Label/manifest rendering | PDF Generator on Linode | UI order payload, Linode filesystem |
| Shared category/template maintenance | PlaceWellAdmin locally | Source repositories |

## 9. Key architectural consequences

1. **Linode availability affects online metadata and order import, not already-saved local labels.**
2. **Firestore availability affects allocation, lookup, public scan resolution and bulk import.**
3. **A user can open and edit locally saved labels without Firestore.**
4. **A first-time label scan can pass local HMAC validation even if lookup is temporarily unavailable, but may have less issued metadata.**
5. **Deleting Firestore allocation records is serious:** physical QR codes cannot be reissued with different random IDs, which is why backups and retained allocation artifacts matter.
6. **Moving Firestore later changes the QR Service persistence layer; the app should continue to call the API rather than connect directly to any database.**
